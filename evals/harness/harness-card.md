# HarnessCard

- **Card version:** 1
- **Corpus version:** harness-corpus-v1
- **Evaluator version:** phase0-contract-v1
- **Runtime revision:** `<required>`
- **Provider / requested model / resolved model:** `<required>`
- **Pricing version:** `<required>`
- **Evaluation date / stable seed:** `<required>`
- **Environment profile and fingerprint:** `<required>`
- **Tool-surface profile and fingerprint:** `<required>`
- **Prompt, skill, context, model-adapter, hook, verifier, and delegation fingerprints:** `<required>`
- **Fixture revisions and digests:** `<required>`
- **Budget fingerprint:** `<required>`
- **Arms and randomized order:** `<required>`
- **Noise-floor result:** `<required>`
- **Raw and per-slice report:** `<required>`
- **Primary and guardrail results:** `<required>`
- **Unknown, timeout, evaluator-failure, and setup-failure counts by arm:** `<required>`
- **Harness complexity delta:** `<required>`
- **Known limitations and blocked surfaces:** `<required>`
- **Decision:** `promote | hold | reject | rollback | insufficient-evidence`
- **Rollback target and trigger:** `<required>`
- **Anti-drift answer:** Does the evidence show better outcomes rather than more agent activity?

External benchmark results are listed separately and cannot replace the local held-out gate.
