/**
 * The rubricator's marks.
 *
 * Rubricated manuscripts set the body in black ink and the marks a reader
 * navigates by in red. The TUI does the same: these are the only glyphs that
 * take the accent, and each has one job.
 */

/** Opens a user turn, and prompts the composer you write the next one in. */
export const PILCROW = '¶';

/** Precedes an H1 or H2 heading in an answer. */
export const SECTION_SIGN = '§';
